<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- ✅ CRON pour les rappels automatiques -->
        <record id="cron_send_action_reminders" model="ir.cron">
            <field name="name">Envoyer les rappels d'actions</field>
            <field name="model_id" ref="risk_management.model_risk_corrective_action"/>
            <field name="code">model._cron_send_reminders()</field>
            <field name="state">code</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <!-- ❌ SUPPRIMER : numbercall n'existe plus -->
            <!-- <field name="numbercall">-1</field> -->
            <!--field name="doall" eval="False"/-->
            <field name="active">True</field>
        </record>

    </data>
</odoo>