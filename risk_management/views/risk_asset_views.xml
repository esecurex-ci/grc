<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_risk_asset_list" model="ir.ui.view">
        <field name="name">risk.asset.list</field>
        <field name="model">risk.asset</field>
        <field name="arch" type="xml">
            <list>
                <field name="code"/>
                <field name="name"/>
                <field name="asset_type" widget="badge"/>
                <field name="criticality_tier" widget="badge"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_risk_asset_form" model="ir.ui.view">
        <field name="name">risk.asset.form</field>
        <field name="model">risk.asset</field>
        <field name="arch" type="xml">
            <form>

                <sheet>

                    <div class="oe_title">
                        <h1>
                            <field name="name"/>
                        </h1>
                    </div>

                    <group>

                        <group>
                            <field name="code"/>
                            <field name="asset_type"/>
                            <field name="criticality_tier"/>
                        </group>

                        <group>
                            <field name="active"/>
                            <field name="process_ids" widget="many2many_tags"/>
                        </group>

                    </group>

                    <notebook>

                        <page string="Description">
                            <field name="description"/>
                        </page>

                    </notebook>

                </sheet>

            </form>
        </field>
    </record>

    <record id="view_risk_asset_search" model="ir.ui.view">
        <field name="name">risk.asset.search</field>
        <field name="model">risk.asset</field>
        <field name="arch" type="xml">
            <search>

                <field name="code"/>
                <field name="name"/>

                <filter name="application"
                        string="Applications"
                        domain="[('asset_type','=','application')]"/>

                <filter name="server"
                        string="Serveurs"
                        domain="[('asset_type','=','server')]"/>

                <filter name="database"
                        string="Bases de données"
                        domain="[('asset_type','=','database')]"/>

                <filter name="tier1"
                        string="Tier 1 - Critique"
                        domain="[('criticality_tier','=','tier1')]"/>

                <separator/>

                <filter name="group_criticality"
                        string="Niveau de criticité"
                        context="{'group_by':'criticality_tier'}"/>

            </search>
        </field>
    </record>

</odoo>