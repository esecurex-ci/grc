<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_risk_loss_list" model="ir.ui.view">
        <field name="name">risk.loss.list</field>
        <field name="model">risk.loss</field>
        <field name="arch" type="xml">
            <list>

                <field name="incident_id"/>
                <field name="date"/>
                <field name="amount"/>
                <field name="recovered_amount"/>
                <field name="net_loss"/>

            </list>
        </field>
    </record>

</odoo>